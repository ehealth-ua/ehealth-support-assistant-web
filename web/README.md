# eHealth Portal (web)

Minimal Next.js + TypeScript + Tailwind scaffold created from `project.prompt`.

Quick start:

1. cd web
2. npm install
3. npm run dev

Notes:
- Language switching is a placeholder. Integrate `next-i18next` or similar for production.
- Add backend API routes for NotebookLM and Helpdesk proxy.
